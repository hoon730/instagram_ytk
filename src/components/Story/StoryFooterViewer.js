import React from "react";
import styled from "styled-components";

const StoryFooterViewer = () => {
  return (
    <Wrapper>
      <FooterArea></FooterArea>
    </Wrapper>
  );
};

export default StoryFooterViewer;
